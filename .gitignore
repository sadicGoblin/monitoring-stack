# Credenciales reales — nunca subir
.env
prometheus/secrets/
alertmanager/secrets/

# Builds del plugin de WordPress (se generan con zip -r)
*.zip

# Datos locales si algún día cambias los named volumes por bind mounts
data/
