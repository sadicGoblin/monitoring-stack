global:
  scrape_interval: 30s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  # Auto-monitoreo del propio Prometheus
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # ── Chequeos externos de sitios web (WordPress o cualquier página) ──
  - job_name: blackbox_http
    metrics_path: /probe
    params:
      module: [http_2xx]
    static_configs:
      - targets:
          # ▼▼ AGREGA AQUÍ LAS URLs A MONITOREAR ▼▼
          # No importa dónde esté alojado cada sitio: el chequeo es una
          # petición HTTP externa, como la de un visitante real.
          - https://favric.cl
          - https://laderasdelrupanco.cl    # WordPress en este servidor
          - https://idrispatagonia.com     # WordPress en otro hosting
          - https://patagoniafarming.com   # WordPress en otro hosting
          - https://marandino.com
    relabel_configs:
      - source_labels: [__address__]
        target_label: __param_target
      - source_labels: [__param_target]
        target_label: instance
      - target_label: __address__
        replacement: blackbox:9115

  # ── (Opcional) Servicios internos del servidor sin URL pública ──
  # Si algún día quieres vigilar los backends de tu docker ps (ej: el
  # puerto 8000 de ms-catalogue), se agrega otro job igual al de arriba
  # pero con targets tipo http://host.docker.internal:<puerto> y módulo
  # http_alive. Avísame y lo armamos.

  # ── Fase 2: aquí irá el scrape del plugin de WordPress ──
  # - job_name: wordpress_plugin
  #   metrics_path: /wp-json/tu-monitor/v1/metrics
  #   scheme: https
  #   authorization:
  #     credentials: <token-del-sitio>
  #   static_configs:
  #     - targets: ["cliente-wordpress.com"]
